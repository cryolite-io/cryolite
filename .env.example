# ============================================================================
# CRYOLITE Environment Configuration
# ============================================================================
# Copy this file to .env and adjust values as needed:
#   cp .env.example .env
#
# IMPORTANT: Never commit .env to version control (it's in .gitignore)
# ============================================================================

# ----------------------------------------------------------------------------
# SonarCloud Configuration
# ----------------------------------------------------------------------------
# Get your token from: https://sonarcloud.io/account/security
SONAR_HOST_URL=https://sonarcloud.io
SONAR_TOKEN=your_sonarcloud_token_here
SONAR_PROJECT_KEY=cryolite-io_cryolite
SONAR_ORGANIZATION=cryolite-io

# ----------------------------------------------------------------------------
# Docker Services
# ----------------------------------------------------------------------------
POLARIS_HOST=localhost
POLARIS_PORT=8181
MINIO_HOST=localhost
MINIO_PORT=9000
MINIO_CONSOLE_PORT=9001
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# ----------------------------------------------------------------------------
# Polaris Bootstrap Configuration
# ----------------------------------------------------------------------------
# These credentials are set at Polaris startup via POLARIS_BOOTSTRAP_CREDENTIALS
# You can use simple, predefined credentials for local development
POLARIS_REALM=POLARIS
POLARIS_ROOT_CLIENT_ID=polarisadmin
POLARIS_ROOT_CLIENT_SECRET=polarisadmin

# ----------------------------------------------------------------------------
# Polaris Catalog Configuration
# ----------------------------------------------------------------------------
# Note: Uses standard s3:// URI scheme (compatible with AWS S3)
# Iceberg's S3FileIO supports s3://, s3a://, and s3n:// schemes
POLARIS_CATALOG=cryolite_catalog
POLARIS_BUCKET=cryolite-warehouse
POLARIS_USER=cryolite_user

# ----------------------------------------------------------------------------
# Polaris User Credentials
# ----------------------------------------------------------------------------
# These are auto-generated by the polaris-setup container on first run
# After running 'docker-compose up -d', get the credentials with:
#   docker logs cryolite-polaris-setup | grep "POLARIS_CLIENT_ID"
#
# Then paste them here:
POLARIS_CLIENT_ID=
POLARIS_CLIENT_SECRET=
POLARIS_WAREHOUSE=cryolite_catalog
POLARIS_SCOPE=PRINCIPAL_ROLE:ALL

# ----------------------------------------------------------------------------
# Development
# ----------------------------------------------------------------------------
JAVA_VERSION=21
MAVEN_VERSION=3.9.12

